# Plantilla de MASTER.md (`navori master template master`)

Mismas 18 secciones que la plantilla `plan` (design.md D3, R30). Cada sección, salvo 'Preguntas abiertas', TERMINA con una línea `Origen:` (`plan<n> §<sección>`, varios planes si coincidieron, o un `D<n>` de esta etapa / `<NN-slug>/D<n>` de una etapa cerrada). 'Preguntas abiertas' dice `Ninguna` (R32) y no lleva `Origen:`.

## Metadatos

Proyecto, etapa, fecha, modo, archivos de `context/md/` consolidados.

Origen: <plan(es) o D<n>>

## Resumen ejecutivo

Qué se construye y por qué.

Origen: <plan(es) o D<n>>

## Estado actual vs. objetivo
<!-- only-mode: en-curso -->

Solo en modo `en-curso`.

Origen: <plan(es) o D<n>>

## Alcance (MoSCoW)

Must / Should / Could / Won't consolidados.

Origen: <plan(es) o D<n>>

## Actores y permisos

Origen: <plan(es) o D<n>>

## Reglas de negocio

`RN-<n>` consolidadas.

Origen: <plan(es) o D<n>>

## Requisitos funcionales

`RF-<n>` consolidados.

Origen: <plan(es) o D<n>>

## Requisitos no funcionales

`RNF-<n>` consolidados.

Origen: <plan(es) o D<n>>

## Dominio y datos

Origen: <plan(es) o D<n>>

## Arquitectura

Origen: <plan(es) o D<n>>

## Stack y librerías

Origen: <plan(es) o D<n>>

## Contratos

Origen: <plan(es) o D<n>>

## Seguridad

Origen: <plan(es) o D<n>>

## Infraestructura y operación

Origen: <plan(es) o D<n>>

## Entrega en partes

Región `navori:master-parts` (la genera `navori master status`, spec 0034 T8): cada `P<n>` con objetivo, alcance, fuera de alcance, dependencias, requisitos semilla, criterios `P<n>.A<m>` y, en `en-curso`, su estado.

Origen: <plan(es) o D<n>>

## Testing

Origen: <plan(es) o D<n>>

## Riesgos

Origen: <plan(es) o D<n>>

## Preguntas abiertas

Ninguna
