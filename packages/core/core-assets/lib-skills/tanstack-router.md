---
name: tanstack-router
description: Use when adding a route, reading params/search, guarding a view, or wiring navigation — TanStack Router (React) file-based routing: route tree, type-safe links, loaders, beforeLoad. Not for React Router (separate skill).
metadata:
  type: reference
---

# TanStack Router — conventions

## When to use this skill

When touching file-based routing: adding a route file, reading a param or search value, guarding a view, or navigating. Target version: TanStack Router 1.170 (React) with `@tanstack/router-plugin` (Vite). On a different major, check the changelog before applying — the `beforeLoad`/`loader` context shape has changed across majors. Not for React Router: that's a separate skill with a different API (loaders return redirects instead of throwing them, no generated route tree).

## The pattern

Routes are files under `src/routes/`; the Vite plugin compiles them into a typed route tree:

```tsx
// src/routes/__root.tsx
export const Route = createRootRoute({
  component: () => <Outlet />,
});

// src/routes/sessions/$id.tsx
export const Route = createFileRoute('/sessions/$id')({
  component: SessionDetailPage,
  beforeLoad: ({ location }) => requireSession(location.href),
});

function SessionDetailPage() {
  const { id } = Route.useParams();   // typed from the file name, no cast
  // ...
}
```

## File conventions

- `__root.tsx` exports `createRootRoute({ component })` rendering `<Outlet />` — the shell every route mounts into.
- Leading underscore (`_layout.tsx`) = a **pathless layout**: wraps children in a shared shell but adds no URL segment.
- `$param.tsx` / `$param/` = a **dynamic segment**, read via `Route.useParams()` — typed, not a raw string cast.
- Leading dash (`-name.tsx`, `-folder/`) = **excluded from routing**, for colocating helpers/components next to the routes that use them.
- `route.tsx` = a folder's layout/config; `index.tsx` = that folder's index route.
- `routeTree.gen.ts` is generated by the plugin on every dev/build. Commit it, **never hand-edit it** — the plugin overwrites it on the next run, silently discarding manual changes.

## Gotchas that bite

- **`beforeLoad` guards THROW `redirect(...)`, never return it.** A guard that `return`s a redirect does nothing — the route loads normally. Guards belong in `beforeLoad`, not `loader`: `loader` runs assuming access already passed.
- **`validateSearch` (typically with Zod) is what makes `Route.useSearch()` typed.** Without it, search params are `unknown` — reading `search.tab` unvalidated is an implicit `any`.
- **`Link to="/sessions/$id"` and `useNavigate()` are checked against the route tree at compile time.** A typo in `to` fails the build — the main payoff of file-based routing, but only if the route tree is up to date.
- **`loader` + TanStack Query goes through router context, not a module-level `queryClient` import**: `createRouter({ routeTree, context: { queryClient } })`, then `loader: ({ context }) => context.queryClient.ensureQueryData(queryOptions)`. This dedupes with the component's `useQuery` for the same key. (Documented pattern from TanStack Router's "External Data Loading" guide — verify it fires as expected in your own routes.)
- **Type registration is required for `Link`/`useNavigate` to type-check.** `main.tsx` needs `declare module '@tanstack/react-router' { interface Register { router: typeof router } }` after `createRouter({ routeTree })` — omit it and every route path degrades to a plain `string`.

## Hard rules

1. Never hand-edit `routeTree.gen.ts`; regenerate it by running dev/build.
2. Auth/role guards live in `beforeLoad` and throw `redirect(...)`; they never return it or live in `loader`.
3. Any route reading query params declares `validateSearch`; no manual `URLSearchParams` parsing in the component.
4. Navigation via `<Link to="...">`/`useNavigate()`, never `window.location`.
5. Route files stay thin: `component` points at a separate page component, not inline JSX with business logic.

## Before declaring done

- No hand-edited `routeTree.gen.ts`; it was regenerated by dev/build and committed.
- Every `beforeLoad` guard throws its redirect; none returns it.
- Search params read through `Route.useSearch()` with a `validateSearch` schema, not parsed by hand.
- `{{qualityGate.fast}}` green.

If any item fails, fix it and re-run the whole list.

<!-- navori:user-section -->
## This repo's routing (your domain)

<!-- user: add here what only applies to THIS repo. Suggestions:
     - The real route map: which paths exist and which layout each hangs from.
     - How the auth guard is implemented and where the redirect lands.
     - Whether loaders are used yet, and if so, the queryClient wiring in the router context.
     - The route-groups/colocation convention for helpers under `-name/` folders.
-->
