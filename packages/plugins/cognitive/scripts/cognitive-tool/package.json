{
  "name": "navori-cognitive-tool",
  "private": true,
  "description": "Isolated toolchain for navori's cognitive-complexity gate. Bootstrap with `bun install` (or npm/pnpm install). Deliberately kept out of the host project's dependencies and eslint config.",
  "dependencies": {
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-sonarjs": "^3.0.0"
  }
}
