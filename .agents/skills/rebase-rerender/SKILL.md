---
name: rebase-rerender
description: "Use when this repo's harness needs re-rendering after a rebase or merge of `main`, or a git conflict lands inside a navori managed block (a ` navori:managed ... ` marker in `.claude/`, `CLAUDE.md`, `AGENTS.md`, etc). Runs the render-and-verify sequence in order. Not for ordinary code conflicts outside a managed block."
---

<!-- navori:managed id="rebase-rerender-local-pointer" hash="25b787f0" version="0.10.0" source="@navori/core" fmkeys="name,description" -->
Project-local skill, maintained in `.claude/skills/rebase-rerender/SKILL.md` (relative to the directory that holds `.agents/`). Read that file before acting; its supporting files resolve relative to its directory. This entry only makes it discoverable here.
<!-- /navori:managed id="rebase-rerender-local-pointer" -->
